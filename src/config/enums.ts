export enum ETaskStatus {
  todo = 'todo',
  inProgress = 'in_progress',
  inReview = 'in_review',
  done = 'done',
}

export enum EPriority {
  low = 'low',
  medium = 'medium',
  high = 'high',
  critical = 'critical',
}
